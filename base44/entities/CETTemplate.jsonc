{
  "name": "CETTemplate",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Template name"
    },
    "unit": {
      "type": "string"
    },
    "timetable": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "time": {
            "type": "string"
          },
          "activity": {
            "type": "string"
          }
        }
      }
    },
    "notes": {
      "type": "string"
    },
    "created_by": {
      "type": "string"
    }
  },
  "required": [
    "name",
    "unit"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "user_role": "instructor"
      },
      "data.unit": "{{user.unit}}"
    },
    "read": {
      "user_condition": {
        "user_role": "instructor"
      },
      "data.unit": "{{user.unit}}"
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}",
          "data.unit": "{{user.unit}}"
        },
        {
          "user_condition": {
            "user_role": "instructor"
          },
          "data.unit": "{{user.unit}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}",
          "data.unit": "{{user.unit}}"
        },
        {
          "user_condition": {
            "user_role": "instructor"
          },
          "data.unit": "{{user.unit}}"
        }
      ]
    }
  }
}