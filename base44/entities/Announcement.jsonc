{
  "name": "Announcement",
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },
    "content": {
      "type": "string"
    },
    "unit": {
      "type": "string"
    },
    "target_role": {
      "type": "string",
      "enum": [
        "all",
        "cadet",
        "instructor"
      ],
      "default": "all"
    },
    "is_active": {
      "type": "boolean",
      "default": true
    },
    "keywords": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Keywords for CET filtering"
    },
    "sent_by": {
      "type": "string"
    }
  },
  "required": [
    "title",
    "content",
    "unit"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "user_role": "instructor"
      },
      "data.unit": "{{user.unit}}"
    },
    "read": {
      "$or": [
        {
          "data.unit": "{{user.unit}}",
          "$or": [
            {
              "data.target_role": "all"
            },
            {
              "data.target_role": "{{user.user_role}}"
            }
          ]
        },
        {
          "user_condition": {
            "user_role": "instructor"
          },
          "data.unit": "{{user.unit}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}",
          "data.unit": "{{user.unit}}"
        },
        {
          "user_condition": {
            "user_role": "instructor"
          },
          "data.unit": "{{user.unit}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}",
          "data.unit": "{{user.unit}}"
        },
        {
          "user_condition": {
            "user_role": "instructor"
          },
          "data.unit": "{{user.unit}}"
        }
      ]
    }
  }
}